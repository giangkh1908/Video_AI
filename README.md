# Growtrics Chemistry Video Request Service

Backend prototype: request → async job → MP4 (visual + audio). See `docs/` for spec, `CLAUDE.md` for architecture.

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
# docs → http://localhost:8000/docs
```
